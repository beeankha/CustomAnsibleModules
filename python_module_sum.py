# This Python module is what the "sum_two_module.py" is based on

def sum(number_one, number_two):
    sum = number_one + number_two
    return sum

number_one = 3
number_two = 5

print(f"The sum of {number_one} and {number_two} is {sum(number_one, number_two)}")
